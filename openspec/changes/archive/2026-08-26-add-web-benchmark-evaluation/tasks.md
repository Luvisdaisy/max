## 1. 基准基础与任务契约

- [x] 1.1 在 `pyproject.toml` 中加入本地 Python Web 测试场所需依赖，并建立 `benchmark` 包结构。
- [x] 1.2 定义带版本号的任务数据模型及 JSON 读取校验，覆盖路由、指令、初始状态、断言、禁令、上限与超时。
- [x] 1.3 编写不少于 100 条 v1 任务，均衡覆盖基础操作、单页操作、多步骤组合与鲁棒性场景。
- [x] 1.4 为任务定义与分类覆盖写离线测试，拒绝缺字段、重复标识和不完整任务集。

## 2. 本地 Web 测试场

- [x] 2.1 实现仅绑定 `127.0.0.1` 的 FastAPI 应用及测试场生命周期入口。
- [x] 2.2 实现任务隔离状态、重置接口与最小评分状态接口，确保连续运行无数据残留。
- [x] 2.3 实现表单页面及其文本输入、选择、校验错误和保存后的可见状态。
- [x] 2.4 实现文件列表页面及其搜索、排序、滚动和模拟文件操作。
- [x] 2.5 实现设置页面及其开关、确认弹窗和保存流程。
- [x] 2.6 为重置、状态读取、业务交互与回环监听限制编写服务级测试。

## 3. 任务执行与自动评分

- [x] 3.1 实现独立评分器，按全部或部分成功断言返回严格成功、断言进度与部分完成率。
- [x] 3.2 实现任务终止控制，区分 Agent 终态、超时、工具调用上限、禁止动作和环境准备失败。
- [x] 3.3 从现有运行事件与终态汇总提取耗时、工具调用、工具失败及已知 Token 用量，未知用量保留为 `null`。
- [x] 3.4 实现逐题 JSON 结果和整次 JSON、Markdown 原子指标报告，不生成不透明综合分。
- [x] 3.5 编写使用 stub 推理客户端和隔离桌面后端的评测链路测试，覆盖成功、部分完成、超限、违规和未知 Token 用量。

## 4. 本地浏览器运行与模型对比

- [x] 4.0 增加 `max --benchmark`、首页 Start 按钮与点击后运行全部 100 条任务的后台协调器。
- [x] 4.1 新增显式本地评测命令，负责启动测试场、重置任务、固定浏览器条件、执行 Agent 与清理资源。
- [x] 4.2 实现基础模型与 LoRA 模型的同条件运行校验，并在报告中记录任务集版本、模型配置和每题结果。
- [x] 4.3 为浏览器前台、窗口条件和环境准备失败提供可读诊断，确保其不计入模型失败率。
- [x] 4.4 补充真实 macOS 浏览器手工验收步骤，验证 Agent 只能通过截图和键鼠完成一条端到端任务。

## 5. 文档与质量验证

- [x] 5.1 编写中文使用文档，说明本地安全边界、任务集结构、启动方式、评分口径和基线/LoRA 对比方法。
- [x] 5.2 在 README 中补充评测能力、运行日志证据与本地浏览器前提说明。
- [x] 5.3 运行 `uv run ruff format src tests scripts`、`uv run ruff check src tests scripts` 和相关 pytest 测试，记录结果。
