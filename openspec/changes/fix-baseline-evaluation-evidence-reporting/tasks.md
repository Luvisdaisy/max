## 1. 任务约束与执行终态

- [x] 1.1 将五题任务 schema 改为固定 180 秒且无动作预算，并更新 loader、任务文件、中文说明与相关测试。
- [x] 1.2 改造基线护栏，移除普通动作计数拒绝，保留 Terminal、WeChat、Reminders 的内容/导航限制和个人写入一次性预算。
- [x] 1.3 用温和中断和有限收尾窗口替换直接超时取消，持久化可用的 Agent 终态、运行摘要与超时原因。
- [x] 1.4 在所有执行出口采集一张只读终态截图，标注截图来源并保存可用于人工复核的运行日志引用。

## 2. 独立评审与结果口径

- [x] 2.1 实现独立截图评审最多三次指数退避重试与 provider 冷却，并记录评审调用/限流错误。
- [x] 2.2 调整结果模型与成功公式：以安全状态和 `pass` 截图评审为准，将 Agent 完成声明降为诊断字段。
- [x] 2.3 补充执行错误、超时、无截图、评审 `fail`、评审 `indeterminate` 和 `review_error` 的稳定终态、中文输出与汇总统计。
- [x] 2.4 为温和中断、超过旧动作预算、成功但未声明、429 与单次截图评审补充 fake Agent/评审测试。

## 3. 单文件记录与报告

- [x] 3.1 用原子 `<timestamp>.json` 替代基线目录内的批次目录和分散 JSON，内嵌参数、逐题记录、汇总与所有可用指标。
- [x] 3.2 提供从单一结果 JSON 读取数据的 Python 报告入口，生成图表 PNG、HTML 和中文摘要到独立 reports 目录。
- [x] 3.3 调整 `--baseline` 和 `--baseline -report` CLI 文案、输出路径与兼容读取逻辑，并更新中文使用文档。
- [x] 3.4 覆盖单 JSON 文件布局、未知 Token、报告生成失败可恢复性及图表/摘要渲染测试。

## 4. 验证

- [x] 4.1 运行 `uv run ruff format src tests` 与 `uv run ruff check src tests`。
- [x] 4.2 运行基线相关 pytest 与完整 pytest，记录组件验证和未执行的真实桌面验证边界。
- [ ] 4.3 在用户明确启动后运行一轮真实桌面基线，核对单一 JSON、五题截图评审和自动图表报告。
