## 1. 模型发现与运行时选择

- [x] 1.1 实现受限于仓库 `model/` 的递归模型发现，并为包含 `config.json` 的目录生成排序后的相对名称。
- [x] 1.2 将 `LocalChatRuntime` 改为维护当前模型选择、按选择验证与懒加载，并保留现有 Qwen3.5-2B 默认选择。
- [x] 1.3 实现模型切换时的历史、模型和处理器重置；加载或生成失败时保留选择并报告本地错误。
- [x] 1.4 为发现、空清单、无效选择、默认选择、成功切换和切换后历史隔离添加运行时单元测试。

## 2. 交互命令接入

- [x] 2.1 扩展 `console_core`，将 `/model` 与 `/model <模型目录>` 分发为可测试的统一操作结果，并保持未知命令行为。
- [x] 2.2 在 CLI 组合根目录、运行时和模型命令回调，确保命令不触发普通聊天推理。
- [x] 2.3 更新 Textual 前端状态文案与输入提示，并渲染模型清单、选择成功和选择失败结果。
- [x] 2.4 为核心分发、CLI 装配和 Textual 斜杠命令路径添加测试。

## 3. 文档与验证

- [x] 3.1 更新 `tech-design.md`，区分已实现的模型选择工作流与不在范围内的下载、远程回退和多模型并行能力。
- [x] 3.2 运行受影响单元测试、完整单元测试、Ruff 格式与检查、`python -m pip check`、`git diff --check` 及 OpenSpec 严格校验，并记录结果。
