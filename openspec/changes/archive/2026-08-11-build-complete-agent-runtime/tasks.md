## 1. 运行时契约与隔离基础

- [x] 1.1 在独立 worktree 中确认当前分支基于已完成聊天控制台变更的 `main@27c46bc`，记录基线并先运行现有控制台测试；实施期间不得修改 `console_core.py`、`console_frontends.py`、`local_llm.py`、Textual widget 或旧 `ReadOnlyToolExplainer`。
- [x] 1.2 定义带完善中文注释的任务请求、任务级消息上下文、最小挂起状态、脱敏 ReAct 审计轨迹、运行事件、最终结果、模型/工具/动作/纠正/恢复预算、取消令牌、标准化观察、桌面动作、批准动作和执行回执模型，并验证工作上下文与审计投影严格分离。
- [x] 1.3 扩展工具契约与注册表，使工具声明权限、允许阶段、超时和可恢复属性，并通过 `ToolContext` 获得任务作用域资源、预算、取消和事件能力；验证阶段越权、未知工具、无效参数和异常均返回稳定失败码。
- [x] 1.4 实现任务级 `InMemoryResourceStore` 和不可猜测 `ResourceRef`，覆盖图像创建、类型校验、跨任务拒绝、释放后拒绝和终态清空测试，确认资源对象不会进入模型 JSON 或审计序列化。
- [x] 1.5 实现桌面会话锁和输入状态清理接口的可注入版本，使用 mock 覆盖锁竞争、重复释放、部分清理失败和异常路径继续清理。

## 2. OCR 主路径感知调度

- [x] 2.1 改造 `observe_screen` 以写入任务内存资源并返回图像引用、捕获时间、显示器物理边界、DPI 和前台窗口摘要，同时保持不落盘和无输入行为。
- [x] 2.2 新增只读顶层窗口发现工具，返回任务栏可见性、前台状态、边界以及 `HWND + PID + resolved executable path`，并测试无权限、窗口消失和多窗口结果。
- [x] 2.3 改造 OCR、预处理、模板匹配和 SoM 工具以消费当前任务的图像引用；保持底层适配器可注入，并验证跨任务引用和第三方异常均不会触发重新截图或下载。
- [x] 2.4 扩展可选 UIA 观察以返回稳定元素标识、焦点状态和受限 Text/Value 证据，确保失败时只返回辅助观察错误，不激活窗口也不阻断截图与 OCR 主路径。
- [x] 2.5 实现标准化观察组合、状态指纹、变化区域和 OCR 元素坐标回映；首帧全屏 OCR，后续仅在窗口、DPI 和捕获范围未变化时复用未变化区域结果。
- [x] 2.6 增加合成图像和 mock 窗口测试，验证确定性执行 `observe_windows + observe_screen → recognize_text`，以及 UIA、模板与 SoM 只在显式增强请求时调用。

## 3. 本地模型 text-or-tool 响应

- [x] 3.1 定义前端无关的 `AgentModelSession` 协议和离线本地实现，按当前选择模型名称懒加载并复用一个模型实例，模型选择变化时在下一任务前释放旧引用，且不调用或修改 `LocalChatRuntime`。
- [x] 3.2 定义并实现 `FinalTextResponse | ToolUseResponse` 判别联合：每轮只允许最终文本或一个工具名与参数；拒绝同时返回文本与工具、多工具列表以及 schema 外字段。
- [x] 3.3 实现任务级模型消息构造，将系统提示、用户消息、模型工具调用和对应工具结果按顺序组织；只在工具结果需要时附加内存图像或结构化观察，不把内部消息写入 `LocalChatRuntime` 历史。
- [x] 3.4 实现模型响应 schema 与所选工具输入 schema 的一次格式纠正和第二次失败终止，测试 Markdown、Thought/Action 文本、隐含 JSON、多工具列表、越界坐标、空文本、空输入和非白名单按键均不得被猜测为调用。
- [x] 3.5 实现快速与审慎模式的上下文裁剪、禁止重复行为和至多一次审慎模型轮次预算，确认模式切换不会扩大公开工具集合或权限。
- [x] 3.6 使用 fake 模型会话验证模型不可用、CUDA/显存错误和视觉能力错误返回 `MODEL_UNAVAILABLE`，且不访问网络、不下载模型也不发送桌面输入。

## 4. Guard、桌面执行与会话安全

- [x] 4.1 实现 Guard 策略和批准哈希，校验动作 schema、窗口三元组、状态指纹、元素或坐标、物理像素转换、允许系统表面、时间有效期、动作间隔和高影响分类。
- [x] 4.2 实现执行前窗口与焦点二次复核，测试审批后窗口切换、元素消失、DPI 变化、坐标越界或批准超时均拒绝输入并要求重新观察。
- [x] 4.3 实现可注入的桌面执行适配器与 `execute_action` 工具，支持 `activate_window`、点击、双击、Unicode 文本、滚动、拖拽、等待和静态白名单 `key_chord`，明确不提供剪贴板、Shell、任意脚本或进程管理。
- [x] 4.4 实现普通动作和高影响动作策略，验证明确的记事本未保存缓冲区追加无需额外控制开关，而保存、覆盖、发送、删除、支付、凭据和关闭未保存内容进入 `WAITING_USER`。
- [x] 4.5 接入 Fail-safe、紧急中止和统一输入状态释放，使用 mock 与现有受控 Win32 探针覆盖成功、异常、超时、取消和进程退出路径，不操作真实业务窗口。

## 5. 验证、恢复与脱敏归档

- [x] 5.1 实现执行后的强制重新观察与 `verify_result` 工具，按任务谓词、窗口/焦点、OCR/UIA、局部变化和本地模型辅助的固定优先级返回结构化证据。
- [x] 5.2 实现记事本追加谓词，验证目标窗口置前、焦点未转移且重新 OCR 的可见内容包含用户新增文本；执行回执或字符数量本身不得判定成功。
- [x] 5.3 实现状态指纹、变化占比、等价工具参数和动作摘要驱动的无进展检测，覆盖第一次重新观察、第二次至多一次审慎模型轮次、第三次 `WAITING_USER`/`NO_PROGRESS` 和禁止无限重复工具或动作。
- [x] 5.4 实现只返回“追加失败证据后继续模型循环”、`reobserve`、`deliberate_reason`、`wait_user` 或 `fail` 的恢复工具，验证恢复不调用执行器且模型产生的任何新有副作用工具调用重新经过 Guard。
- [x] 5.5 实现终态 `archive_run`，复用 `artifacts/` 归档配置、环境摘要、轨迹和结果；增加敏感假数据测试，确认截图、完整 OCR、输入文本、模型原始响应、密码和剪贴板不落盘。

## 6. 完整受控 ReAct Runtime 与状态图

- [x] 6.1 将 LangGraph 重构为 `REASON → final_text | TOOL_GATE` 循环：只读工具走 `EXECUTE_TOOL → APPEND_RESULT → REASON`，有副作用工具走 `GUARD → ACT → OBSERVE_AFTER_ACTION → VERIFY → APPEND_RESULT → REASON`，并保留恢复、等待用户、清理、归档与终态路由。
- [x] 6.2 实现 `AgentRuntime.run(request, emit=None)` 与挂起任务恢复入口，统一创建任务级消息上下文、检查模型/工具/动作/纠正/恢复/时间预算、按需获取锁、调用图和捕获异常；在终态及 `WAITING_USER` 路径清理输入、资源和锁，恢复时加入用户消息并使旧坐标与批准失效。
- [x] 6.3 实现工具门：仅向模型公开允许工具，验证名称、输入 schema、权限、阶段和预算；拒绝模型直接选择 Guard、验证、恢复、归档、资源清理或操作系统 API。
- [x] 6.4 实现工具结果追加器：只读结果直接回填任务消息；有副作用结果必须组合执行、动作后观察和验证的净化信息后回填；审计轨迹只保存白名单投影且不保存模型思维链。
- [x] 6.5 实现前端无关的净化模型轮次与工具事件，测试同步无回调和带回调运行结果一致，事件只包含阶段、工具名、状态、耗时和允许说明。
- [x] 6.6 覆盖首轮文本成功、多轮只读工具、动作工具必经 Guard、动作后验证、未验证最终文本拒绝、非法工具与参数、确认等待与恢复、各类预算耗尽、循环检测、取消、超时、Fail-safe、清理失败、归档失败和会话忙碌的完整 mock 状态图测试；明确断言普通问答的观察工具和桌面锁调用数均为零。
- [x] 6.7 装配默认注册表中的全部感知、模型、审批、控制、验证、恢复与归档能力，明确模型可见工具子集和内部能力子集，并验证有副作用工具只能经固定 Guard/ACT 路径调用。

## 7. 记事本纵向验收

- [x] 7.1 扩展受控 Win32 夹具以模拟任务栏可见窗口、可聚焦编辑区、可读取文本和未保存缓冲区，完成无需真实用户文件的自动端到端运行时测试。
- [x] 7.2 以确定性 fake 模型验证完整 text-or-tool 轨迹：依次调用窗口发现、截图和 OCR，调用置前、聚焦、`Ctrl+End` 与 Unicode 追加工具，消费动作后观察与验证结果，最后返回最终文本。
- [x] 7.3 覆盖多个记事本窗口、OCR 无匹配、焦点转移、新增内容缺失、窗口在审批后变化和用户未提供新增文本时进入人工处理的失败场景。
- [x] 7.4 在授权交互式 Windows 会话中使用专用未保存记事本缓冲区和唯一测试标记进行一次真实人工验收，确认任务栏定位、置前、OCR、追加与验证成功且不保存文件，并将脱敏诊断证据写入 `artifacts/`。

## 8. 现有聊天控制台薄适配

- [x] 8.1 在 `main@27c46bc` 上先运行控制台、LocalChatRuntime、只读工具和 Textual 测试，记录 `_interactive_dispatch`、`EventSink`、`ConsoleEvent`、`SessionStatus` 与单活动请求门禁的稳定基线。
- [x] 8.2 仅在 `cli.py` 依赖装配中把普通消息改为调用或恢复 `AgentRuntime`，通过可调用模型名称解析器跟随 `/model` 选择，并将净化模型轮次映射到现有 NOTICE、工具进度映射到现有 TOOL、最终文本交给现有 ASSISTANT 事件；不修改 Textual widget、`console_core.py`、`console_frontends.py`、`local_llm.py` 或旧解释器。
- [x] 8.3 让 `max-agent` 和 `max-agent --chat` 默认无需 `--enable-desktop-control` 即可处理受 Guard 管理的桌面目标，并通过现有 `SessionStatus` 适配器报告“本地、Agent Guard、受控桌面、无网络”的真实边界。
- [x] 8.4 将 `/clear` 装配为同时清除本地历史和挂起 Agent 任务，将成功 `/model` 切换装配为使旧 Agent 模型会话与挂起任务失效，并让 `/quit` 取消和清理活动或挂起任务；保持命令的现有界面行为。
- [x] 8.5 增加适配与回归测试，确认每次提交只创建一个运行时调用、有限多轮工具调用仍占用同一 UI 请求、`WAITING_USER` 返回后恢复编辑器且下一条消息恢复同一任务、普通问答绝不调用观察工具或夹带屏幕理解、单活动请求门禁与焦点恢复不变、工具事件不泄漏数据，且 `/model`、`/doctor`、`/clear`、`/status`、`/help`、`/quit` 均保持可用。

## 9. 文档与最终验证

- [x] 9.1 按当前实际实现更新中文 `README.md` 和 `tech-design.md`，将当前已实现的 mock 固定状态机与聊天一次只读工具循环同未来受控 ReAct 运行时分开描述，并说明 text-or-tool 消息循环、普通对话零桌面观察、OCR 按需主路径、Guard、动作后验证、对话恢复、记事本验收和仍未实现能力。
- [x] 9.2 运行 `ruff format --check src tests`、`ruff check src tests`、`python -m unittest discover -s tests -v`、`python -m pip check` 和 `max-agent doctor --artifact-root artifacts`，并报告所有结果。
- [x] 9.3 运行仓库指定版本的 OpenSpec 变更校验与 `openspec validate --specs`，核对代码、自动化测试、真实命令输出和 `tech-design.md` 的当前实现表述一致后，才勾选剩余任务。
