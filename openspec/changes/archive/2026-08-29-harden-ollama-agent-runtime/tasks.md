## 1. 上下文预算与配置

- [x] 1.1 为 provider 与 Settings 增加 262144 Ollama 上下文容量、输出预留和安全余量，并校验非法预算。
- [x] 1.2 实现保守 token 估算和按预算选择完整工具链，请求发送 `max_tokens` 并记录脱敏预算诊断。

## 2. 工具运行时契约

- [x] 2.1 为 Tool 增加副作用元数据和递归严格 schema，注册表支持按名称筛选和元数据查询。
- [x] 2.2 在注册表中实现受限 JSON Schema 校验、真实工具超时和带稳定错误码的结构化 ToolResult。

## 3. Agent 阶段与完成验证

- [x] 3.1 根据活动截图和任务状态动态选择工具，并在 act 分发时再次拒绝未开放工具。
- [x] 3.2 限制每批最多执行一个副作用，为所有被阻断调用生成配对 tool 结果。
- [x] 3.3 增加 task_complete、可恢复完成门状态和 observe 后完成路由，副作用后无声明不得直接 done。

## 4. 验证与文档

- [x] 4.1 补充 256K 预算、动态工具、严格参数、工具超时、多副作用和完成门行为测试。
- [x] 4.2 更新 `.env.example` 与 README，说明 Ollama 256K 配置、上下文预算和完成门。
- [x] 4.3 运行 Ruff format/check、相关测试、全量 pytest 和当前活动 OpenSpec 的严格校验。
