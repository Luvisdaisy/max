## 1. 配置与重试模型

- [x] 1.1 在配置中增加 `MAX_GUI_INFERENCE_MAX_RETRIES`，默认 5，并校验整数范围 0–5
- [x] 1.2 定义带中文文档的重试通知、错误分类结果与最终异常元数据，保证不保存请求载荷或密钥
- [x] 1.3 实现 HTTP 状态、400 结构化正文和 `httpx` 传输异常的可重试分类，并保留安全错误摘要

## 2. 推理客户端重试

- [x] 2.1 重构 `InferenceClient.stream`，在同一份 URL、headers 与 payload 上执行首次请求及最多 5 次重试
- [x] 2.2 实现 0.5/1/2/4/8 秒有界指数退避、20% 抖动、`Retry-After` 与 30 秒等待上限
- [x] 2.3 让退避等待响应用户中断，并保证中断后不再发起网络请求
- [x] 2.4 为每次尝试隔离流式与工具调用累加状态，只允许在首个有效 SSE 增量前重放
- [x] 2.5 在重试耗尽和流中途断开时返回包含尝试次数、最后状态与安全正文摘要的中文错误

## 3. Agent、事件与 TUI

- [x] 3.1 把推理客户端重试通知接入 `AgentRunner`，新增 `model.retrying` 事件并补齐终态的 `attempt_count`、`retry_count`
- [x] 3.2 保持重试属于一次逻辑模型调用，不增加 ReAct iteration、模型调用汇总或 token 用量，不重新执行工具
- [x] 3.3 在 TUI 运行记录与监控面板显示中文重试次数、原因和下一次等待，并保持中断状态语义
- [x] 3.4 验证重试成功只提交一条助手消息，最终失败只更新一次会话错误和检查点且不重复已有工具消息

## 4. 自动化测试

- [x] 4.1 增加配置默认值、0 次关闭、边界值和非法值测试
- [x] 4.2 增加连接错误、超时、408/425/429/5xx、成功恢复和最多 6 次网络尝试的客户端测试
- [x] 4.3 增加不透明 400、瞬时结构化 400、永久 400、401/403/404/413/422 不重试测试
- [x] 4.4 增加退避序列、抖动范围、`Retry-After`、等待上限和退避期间中断测试
- [x] 4.5 增加首个增量前断流可重试、正文/reasoning/工具分片后断流不可重试和累加状态隔离测试
- [x] 4.6 增加 `model.retrying` 顺序与脱敏、逻辑调用统计、会话消息幂等和桌面工具不重放测试

## 5. 文档与验证

- [x] 5.1 更新 `.env.example` 和 `README.md`，说明默认 5 次重试、总尝试数、可重试边界、关闭方式与流中途限制
- [x] 5.2 运行推理、Agent、可观测性、会话与 TUI 相关测试，并区分组件验证与真实 Ollama 端到端验证
- [x] 5.3 运行 `uv run ruff format src tests`、`uv run ruff check src tests`、完整测试与 `openspec validate add-inference-retry-policy --strict`
