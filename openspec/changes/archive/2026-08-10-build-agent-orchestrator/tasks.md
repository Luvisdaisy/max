## 1. 状态与提示词契约

- [x] 1.1 新增可序列化的编排任务请求、结果、步骤记录、终止原因和脱敏回执摘要契约。
- [x] 1.2 新增 LangChain `ChatPromptTemplate` 提示词构造，覆盖快速与审慎规划上下文，且不加载或调用模型。
- [x] 1.3 为状态契约和提示词构造添加单元测试，验证必填字段、敏感文本脱敏和模式差异。

## 2. LangGraph 编排器

- [x] 2.1 新增 `orchestration/graph.py`，装配单任务 `observe → plan → guard → act → verify` 显式状态图与终态。
- [x] 2.2 通过工具注册表适配观察、规划、审批、执行、验证、恢复和归档回执，拒绝直接调用桌面、视觉或模型 SDK。
- [x] 2.3 实现工具失败、用户取消、预算耗尽和人工处理的条件边与可审计终止结果。
- [x] 2.4 实现无进展恢复计数：重新观察、一次审慎重规划、随后 `WAITING_USER`，并阻止无限循环。

## 3. 状态机验证

- [x] 3.1 使用 mock 注册表工具测试成功路径、工具失败和取消路径，确认执行前必须经过审批。
- [x] 3.2 测试预算耗尽、连续无进展、审慎重规划和人工等待的状态转换与回执摘要。
- [x] 3.3 验证编排器结果和历史不包含图像对象或输入文本明文。

## 4. 文档与质量核验

- [x] 4.1 更新 `tech-design.md` 的当前实现基线和模块职责，明确首版编排器仅使用 mock/注册表工具，不启用模型或桌面控制。
- [x] 4.2 运行 `python -m unittest discover -s tests -v`、`python -m pip check` 和 Ruff；记录不启用桌面控制的编排器验证结果。
