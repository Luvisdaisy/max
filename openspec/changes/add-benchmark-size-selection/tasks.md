## 1. 批次选择与评测协调

- [x] 1.1 在任务集模块实现并验证仅接受 10 或 100 的确定性批次选择函数；10 条按原始顺序选取 4 easy、3 medium、3 hard。
- [x] 1.2 调整 `BenchmarkController`，使其以选定任务批次初始化进度并只遍历该批次，同时保持运行中拒绝再次启动的互斥行为。
- [x] 1.3 让报告汇总写入请求任务数，并按 10/100 使用不同报告路径，保留既有逐题指标和未知 Token 的 `null` 语义。

## 2. 首页入口

- [x] 2.1 调整评测首页和启动接口：显示「运行 10 条」与「运行 100 条」按钮，并正确呈现所选进度和报告路径。
- [x] 2.2 保持默认 TUI 与 `max --benchmark` 的回环服务边界不变；评测规模不得新增 CLI 参数。

## 3. 测试与文档

- [x] 3.1 为任务选择、控制器状态、首页两个 POST 入口与运行中冲突补充离线合同测试。
- [x] 3.2 为首页的 10/100 启动、非法数量与运行中冲突补充离线合同测试，确保首页之外不存在批次选择入口。
- [x] 3.3 更新 README 与 Web GUI 评测文档，说明首页选择、10 条的难度覆盖、冒烟定位和报告命名。
- [x] 3.4 运行 `uv run ruff format src tests`、`uv run ruff check src tests`、相关 pytest 与 `openspec validate add-benchmark-size-selection --strict`。
