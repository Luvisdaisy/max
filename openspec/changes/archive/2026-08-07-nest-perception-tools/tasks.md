## 1. 结构与文档

- [x] 1.1 更新 `tech-design.md`，将未来感知模块固定为 `tools/perception`，并说明工具契约、注册表和当前未实现边界。
- [x] 1.2 更新 `AGENTS.md` 的验证命令为 `doctor`，并记录未来感知模块的目录约定。

## 2. 受控桌面探针

- [x] 2.1 先补充单元测试，覆盖显式探针请求自身窗口前台后仍必须确认句柄，以及前台不可得时无输入跳过和可操作提示。
- [x] 2.2 以最小实现让显式探针仅对临时 Tk 窗口发出一次有界的前台请求；确认失败时保持跳过，不发出鼠标或键盘事件。

## 3. 验证

- [x] 3.1 运行全量单元测试与 `python -m pip check`。
- [x] 3.2 运行默认 Doctor 与显式桌面探针，确认默认无输入、显式探针的通过或安全跳过输出均易于理解。
- [x] 3.3 验证 OpenSpec 变更并在用户确认后同步规格、归档变更。
