## 1. 本地运行事件基础

- [x] 1.1 新增运行可观测性包及中文模块/类/函数文档，定义 `RunEvent` 公共信封、有限事件类型、运行编号与事件序列规则
- [x] 1.2 实现单次运行 `RunRecorder`：追加 `artifacts/runs/<run-id>.jsonl`、每事件 flush、单调耗时、统计汇总和无限保留行为
- [x] 1.3 实现从既有 JSONL 最后一条合法事件恢复顺序号，容忍异常退出留下的不完整末行，并识别未闭合工具调用但不自动重放
- [x] 1.4 实现记录器故障降级与安全中文诊断，确保目录/序列化/写入失败后停止本次磁盘记录但不抛进 Agent 业务流程
- [x] 1.5 补运行事件与记录器单元测试，覆盖信封字段、严格顺序、增量可见、恢复追加、不完整末行、无限保留和写入失败降级

## 2. Agent 运行上下文与事件边界

- [x] 2.1 为 `AgentState` / checkpoint 增加 `run_id`，新文本回合创建新运行，中断恢复沿用原运行，旧 checkpoint 缺字段时保持兼容
- [x] 2.2 在运行开始/恢复、状态迁移和模型开始/完成/失败边界发出事件；模型完成事件引用已提交助手消息并只记录 reasoning/正文字符数
- [x] 2.3 在 `registry.invoke` 前发出 `tool.started`，工具消息提交后发出完成/失败事件并引用消息下标，不把 reasoning、键盘正文、OCR 全文或图像字节复制进 JSONL
- [x] 2.4 在观察完成和 Agent 终态发出事件，生成模型/工具/迭代/耗时汇总，并把动作后截图表述为观察已捕获而非业务验证成功
- [x] 2.5 保留现有 token、reasoning、status 与 message 回调行为，隔离事件观察者异常，确保日志故障不触发工具重试或改变业务终态
- [x] 2.6 更新 Agent、会话与端到端测试，覆盖不同回合 run_id、中断恢复、事件顺序、消息引用、推理异常、未闭合桌面动作和旧 checkpoint 兼容

## 3. 默认运行监控面板

- [x] 3.1 在 Textual 布局中增加默认可见且独立于会话记录区的运行监控面板，并补齐中文代码文档
- [x] 3.2 消费运行事件并实时展示运行编号摘要、状态、轮次/上限、子任务、活动调用、累计耗时、调用统计和最近事件
- [x] 3.3 对监控摘要实施字段级安全渲染，不展示 reasoning、键盘正文、OCR 全文、密钥、截图字节或运行文件绝对路径
- [x] 3.4 在面板单独展示运行日志写入失败诊断，不把它混入会话消息或误标为工具/任务失败
- [x] 3.5 补 TUI 行为测试，覆盖启动默认显示、工具开始时即时更新、终态摘要、会话记录不受污染、敏感正文隐藏和写入失败时界面可响应

## 4. 说明、兼容与质量验证

- [x] 4.1 更新现有“不得创建 `artifacts/runs/`”相关测试和断言，使会话 `exec` 与独立运行事件按新规范并存
- [x] 4.2 更新 README：说明运行目录、无限保留、磁盘持续增长、reasoning/键盘输入/截图等敏感信息范围及手动清理责任
- [x] 4.3 验证监控功能不启动网络监听、不要求外部服务且不新增不必要的运行时依赖
- [x] 4.4 运行 `uv run ruff format src tests` 和 `uv run ruff check src tests`，修复本次变更引入的格式与 lint 问题
- [ ] 4.5 运行相关定向测试与完整 `uv run pytest`，确认运行事件、会话恢复、TUI 和既有 GUI Agent 行为全部通过
