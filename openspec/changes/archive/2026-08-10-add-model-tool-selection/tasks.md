## 1. 受限工具选择协议

- [x] 1.1 在工具契约和注册表中暴露只读权限、模型可见工具描述及白名单筛选，不改变现有感知工具的调用入口。
- [x] 1.2 定义并实现模型结构化工具选择的解析、单次调用预算、工具名与参数验证，以及可审计的失败回执。
- [x] 1.3 扩展 LangGraph 编排路径，使其把有效工具回执交给模型解释阶段，同时继续从步骤记录和最终结果中剔除图像数据。
- [x] 1.4 为有效选择、未知工具、越权工具、无效参数、预算耗尽和回执脱敏添加单元测试。

## 2. 本地视觉解释与演示

- [x] 2.1 扩展本地模型运行时，支持将内存图像、目标和工具观察传入兼容的已选择视觉模型，并保持纯文本聊天兼容。
- [x] 2.2 将普通 Textual 聊天接入受限工具循环：模型可在一条用户消息内自行选择一次 `observe_screen`，接收内存图像后继续推理并回复；禁止桌面输入、截图落盘和网络回退。
- [x] 2.3 为模拟视觉模型和截图工具的聊天端到端链路添加测试，并在本机视觉模型可加载时通过普通聊天执行一次手动验收。

## 3. 文档与验证

- [x] 3.1 更新 `README.md` 与 `tech-design.md`，说明模型只可选择白名单只读工具、截图仅在内存使用，以及视觉模型兼容性限制。
- [x] 3.2 运行受影响测试、完整单元测试、Ruff、`python -m pip check`、`git diff --check` 和 OpenSpec 严格校验，并记录手动验收结果或阻塞原因。
