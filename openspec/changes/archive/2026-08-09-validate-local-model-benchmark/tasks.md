## 1. 本地输入预检

- [x] 1.1 定义并实现受支持 Qwen3.5-4B 本地目录的最小完整性检查，拒绝目录外或不匹配的模型目标，并提供本地修复提示。
- [x] 1.2 在加载模型前验证用户指定图像存在且可解码，不复制图像内容，并为无效输入提供明确错误。
- [x] 1.3 为模型与图像预检补充成功、缺失文件、错误目录和不可解码图像的单元测试。

## 2. 基准归档与离线执行

- [x] 2.1 调整基准编排顺序，使归档先创建，随后将预检、模型加载和推理纳入统一的阶段化失败记录。
- [x] 2.2 在成功与失败结果中记录稳定阶段、离线状态、计时与显存字段，并净化绝对路径和其他敏感输入信息。
- [x] 2.3 更新 CLI 基准参数到归档的映射，保留本地 `local_files_only` 运行边界且不引入任何网络回退。
- [x] 2.4 为成功和各失败阶段的归档结构、离线约束与敏感路径排除补充自动化测试。

## 3. 开发者验收与文档

- [x] 3.1 更新 `README.md`，说明显式下载、离线 4B 单图基准、证据目录检查及模型和图像不归档的边界。
- [x] 3.2 更新 `tech-design.md` 的当前实现基线与后续路线图区分，准确反映预检和归档行为。
- [x] 3.3 在具备完整本地模型和交互式 GPU 环境时执行一次真实基准；若环境不具备，记录可复现的手动验收命令与限制，且不伪造运行结果。

## 4. 回归验证

- [x] 4.1 运行相关单元测试及完整测试套件，修复由本变更引入的回归。
- [x] 4.2 运行 `python -m pip check`、`max-agent --help`、OpenSpec 变更验证与 `git diff --check`，记录验证结果。
