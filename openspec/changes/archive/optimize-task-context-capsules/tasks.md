## 1. 任务上下文模型与持久化

- [x] 1.1 在 Agent 状态层定义带类型标注和中文文档的任务胶囊、动作摘要与最新观察数据模型，固定近期动作上限为 6。
- [x] 1.2 扩展会话存储以读写可选任务胶囊快照，并兼容缺少该字段的历史会话与 checkpoint。
- [x] 1.3 为胶囊摘要实现敏感数据清理：不保留截图字节、OCR 全文或 `keyboard_type` 输入正文。

## 2. Agent 推理上下文裁剪

- [x] 2.1 在新用户任务、任务完成及中断恢复路径建立或续接任务胶囊，保留现有全量会话消息和 run_id 语义。
- [x] 2.2 从任务胶囊构造每次 think 的消息列表：保留原始指令、计划、最新观察和近期完整工具调用链，不再默认使用全会话消息。
- [x] 2.3 实现近期动作滚动、最新截图优先级与无有效图像回退，并保持现有截图前置的桌面安全 gate。
- [x] 2.4 在编码前校验 tool_call_id 调用／结果配对，确保裁剪不会产生无前序调用的 tool 消息。

## 3. 推理编码与运行诊断

- [x] 3.1 调整多模态编码入口，使其仅按任务上下文选择的最新有效图像内联，旧任务图像不参与最近图选择。
- [x] 3.2 为模型运行事件和终态汇总加入不含正文的上下文规模、裁剪数量与任务标识诊断，并延续未知 token 用量不写零的规则。

## 4. 测试与质量验证

- [x] 4.1 补充 Agent 测试：新任务不重放旧消息、任务内原始指令与工具结果可见、6 条动作上限及 tool_call_id 配对。
- [x] 4.2 补充推理编码测试：当前任务最新截图唯一内联、旧任务截图排除、无图时可正常请求。
- [x] 4.3 补充会话和可观测性测试：任务胶囊兼容读取、中断恢复、敏感键入内容不进摘要，以及裁剪诊断不含正文。
- [x] 4.4 运行 `uv run ruff format src tests`、`uv run ruff check src tests` 与相关 pytest 用例，并记录结果。
