## 1. 任务契约与命令入口

- [x] 1.1 新建版本化五题基线任务 JSON、严格数据模型与 loader，覆盖执行提示词、截图 rubric、前置条件、风险、预算与 nonce 模板。
- [x] 1.2 为 `max` / `max-gui` 新增 `--baseline` 入口、显式轮次数与开始前确认；保持 `--benchmark` 的现有首页语义不变。
- [x] 1.3 实现 macOS 基线环境预检，区分权限、应用、网络、登录、自发会话、取消与安全阻断，并为 T4/T5 建立每轮单次写入护栏。
- [x] 1.4 为任务定义、CLI 参数、确认拒绝、预检分类和写入不重试补充单元测试与中文帮助文案。
- [x] 1.5 将 Terminal、Reminders 与 WeChat 的固定验收文本调整为用户指定值，并同步执行护栏、测试和文档。
- [x] 1.6 每题启动前要求人类确认所需窗口已前置，按任意键继续；未确认时安全阻断该题，并补充测试和文档。
- [x] 1.7 移除批次级确认与硬编码应用预检，保留每题人工窗口确认，并修复批次目录预留与增量结果写入。
- [x] 1.8 将基线执行/评审 LLM 调用限制为最多 3 次指数退避重试；保存错误与尝试次数，并在每题独立评审后输出成功或失败。
- [x] 1.9 新增最多 5 份 `summary.json` 的 Matplotlib 对比报告和受限 `-clean` 入口，补充 CLI、清理、报告与文档测试。

## 2. 执行与独立截图评审

- [x] 2.1 新建基线运行编排器，按固定顺序以任务专属提示词调用既有 `AgentRunner`，并复用运行 JSONL、截图与工具统计。
- [x] 2.2 提取并持久化每题最终截图、Agent 完成声明、执行终态、环境状态和安全/预算状态；无最终截图时不发起评审。
- [x] 2.3 实现全新、无工具的单次截图评审请求与严格 JSON 解析，隔离 Agent 最终文本、对话、reasoning 和工具轨迹。
- [x] 2.4 定义截图评审、环境阻断、评审错误和端到端成功的分母/分子，确保环境或安全状态不误计为模型失败。
- [x] 2.5 使用 fake Agent、fake 环境和 fixture 截图覆盖执行顺序、一次评审、无截图、评审失败、写入预算和未知 Token。

## 3. 指标、结果与可视化

- [x] 3.1 定义 manifest、逐题结果和批次汇总 schema，记录任务/提示词版本、round、模型摘要、执行/评审指标、证据引用和 null 语义。
- [x] 3.2 实现不可覆盖的本地结果目录、JSON/Markdown 汇总与安全的错误脱敏，禁止写入密钥、完整对话、reasoning 或键盘输入正文。
- [x] 3.3 使用 Python 标准库生成离线 `dashboard.html`，按任务与模型可视化多轮成功/阻断/评审结论、耗时、动作、工具失败、已知 Token 和终态分布。
- [x] 3.4 为跨轮聚合、未知值、样本数显示、HTML/SVG 渲染和 Web 评测结果隔离添加测试。

## 4. 文档与验证

- [x] 4.1 编写中文使用文档，说明五题内容、权限/网络/登录前提、云端截图评审风险、显式多轮确认、微信/提醒事项清理和结果阅读方式。
- [x] 4.2 更新 README 的既定评测章节，区分 Web benchmark、真实桌面 baseline、组件验证与真实运行证据。
- [x] 4.3 运行 `uv run ruff format src tests`、`uv run ruff check src tests`、相关 pytest 与完整 pytest，并记录组件验证结果。
- [ ] 4.4 仅在用户明确启动后，在具备 macOS 权限和本人 WeChat 登录的环境完成一轮真实基线评测；人工复核五题证据和 dashboard，未运行的模型/LoRA 对比明确标记为未验证。
