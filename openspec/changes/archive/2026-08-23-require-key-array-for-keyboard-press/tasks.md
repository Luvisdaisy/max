## 1. 按键接口收紧

- [x] 1.1 将 `keyboard_press.keys` 的 schema 和说明改为唯一的非空字符串数组格式。
- [x] 1.2 仅接受字符串数组并保留键白名单、别名与危险热键校验。

## 2. 回归验证

- [x] 2.1 更新既有单键用例，并新增 `Command+Tab` 组合键成功用例。
- [x] 2.2 覆盖字符串、数组文本字符串、空数组和非字符串元素的拒绝路径。
- [x] 2.3 运行 Ruff 格式化、Ruff 检查及相关测试。
