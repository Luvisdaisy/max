## 1. 状态模型与观察接口

- [x] 1.1 在 `agent/context.py` 定义带中文文档的桌面快照、UI 候选、进度项和预期数据模型及清洗／失效函数
- [x] 1.2 实现只读前台应用、窗口、焦点与对话框观察适配器，并为 macOS 无权限、平台不支持和字段缺失提供显式未知状态
- [x] 1.3 将当前帧语义定位／OCR 结果收敛为有上限、无坐标且绑定来源帧的 UI 候选

## 2. ReAct 状态更新与验证

- [x] 2.1 在 `observe` 节点创建或替换桌面状态快照，并在新帧、桌面副作用、任务边界和恢复边界失效旧状态
- [x] 2.2 实现受限 expectation 及其与动作前后快照的确定性验证器，输出已验证／未验证与中文诊断
- [x] 2.3 接入进度项生命周期，使仅通过预期验证的项推进，并协调既有 `plan`、`current_subtask`、恢复护栏和完成声明
- [x] 2.4 将脱敏状态摘要注入 `think` 上下文，确保不输出坐标、输入正文、完整 OCR、历史帧候选或可执行编号

## 3. 会话与工具边界

- [x] 3.1 扩展 Session 与 checkpoint 的状态层持久化、加载清洗和旧格式安全降级
- [x] 3.2 确保新用户任务不继承旧任务桌面快照、进度、预期、UI 候选或可执行事实，同时保留既有只读历史观察规则
- [x] 3.3 让桌面工具将未知前台状态作为诊断信息使用，但不放宽截图、坐标、移鼠和点击门禁
- [x] 3.4 让 `task_complete` 独立完成校验检查必经进度项的验证状态，避免一般预期替代任务完成证据

## 4. 验证与文档

- [x] 4.1 为快照采集、未知状态、候选帧失效、状态恢复和新任务隔离补充单元测试
- [x] 4.2 为 expectation 驱动的进度推进、证据不足不推进、完成校验协同和既有工具门禁补充 Agent 集成测试
- [x] 4.3 使用 fake desktop 与 mock 观察适配器完成跨应用／文件选择器状态流程测试；在真实 macOS 上执行一次只读前台状态冒烟并单独记录结果

验证记录：2026-08-30 在本机 macOS 执行只读观察，结果为 `frontmost_app_unavailable`；实现按设计安全降级为未知身份，未执行任何辅助功能动作。

- [x] 4.4 更新 README 既定章节中的状态层、权限降级和验证边界说明
- [x] 4.5 运行 `uv run ruff format src tests`、`uv run ruff check src tests`、相关 pytest、完整 pytest 与 `openspec validate strengthen-desktop-agent-state --strict`

验证记录：2026-08-30 Ruff format/check 通过；全量 pytest 为 `301 passed, 1 warning`。严格 OpenSpec 校验将在归档前对所有变更统一执行。
