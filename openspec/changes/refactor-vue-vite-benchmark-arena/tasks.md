## 1. 前端工程与单进程托管

- [x] 1.1 在 `frontend/benchmark-arena/` 创建 Vue + Vite 工程、锁定构建依赖，并提供开发与生产构建脚本。
- [x] 1.2 实现 FastAPI 对前端构建产物、首页和已注册业务路由的托管与回退，保持仅监听 `127.0.0.1`。
- [x] 1.3 为缺失构建产物、未知业务路由和前端资源加载失败提供明确的本地错误处理。
- [x] 1.4 为前端构建和单进程静态托管补充自动化验证。

## 2. 后端状态与可见业务 API

- [x] 2.1 定义 v2 评测场的后端状态模型、业务实体和每题隔离重置逻辑。
- [x] 2.2 实现仅供 Vue 页面调用的业务 API，覆盖创建、编辑、筛选、排序、分页、批量操作、确认和校验恢复所需状态变更。
- [x] 2.3 保留并收紧 `/api/reset`、`/api/state`、启动与进度接口，确保评分状态、任务指令和成功断言不通过业务 API 暴露。
- [x] 2.4 为重置隔离、业务状态变化、控制面不可见和评分读取补充 FastAPI 合同测试。

## 3. Vue 评测场页面

- [x] 3.1 实现评测首页及「运行 10 条」「运行 100 条」的可见状态、进度和报告路径展示。
- [x] 3.2 实现支持文本输入、选择、筛选、排序、滚动或分页、编辑与批量操作的业务页面。
- [x] 3.3 实现多步骤工作流、确认流程、校验错误恢复、动态反馈和布局变化的可见交互。
- [x] 3.4 为每个 v2 任务路由提供可渲染页面，并进行浏览器级可见交互验证。

## 4. 分层独立任务集

- [x] 4.1 定义 v2 任务 schema，加入难度、能力标签与任务唯一性所需字段，移除模板复制展开字段和逻辑。
- [x] 4.2 编写 100 条显式且唯一的任务记录，覆盖 easy、medium、hard 的单一操作、同页组合、跨页流程、状态依赖和异常恢复。
- [x] 4.3 实现任务集加载校验：恰好 100 条、唯一 ID、三种难度、规定能力覆盖，以及拒绝复制式字段。
- [x] 4.4 定义并测试固定 10 条冒烟清单，确保其覆盖三种难度和关键能力；保持 100 条返回完整任务集并拒绝其他规模。

## 5. 运行器、评分与报告衔接

- [x] 5.1 将评测服务、运行器和脚本切换到 v2 路由、状态读取与任务集，不改变真实浏览器键鼠、顺序执行和独立评分边界。
- [x] 5.2 保持报告中的请求规模、逐题结果、部分完成率、终止原因、未知 Token 为 `null` 等既有可审计语义。
- [x] 5.3 为 10/100 条启动、运行互斥、任务路由打开、状态评分和报告范围补充回归测试。
- [x] 5.4 保留并验证 `max-gui --benchmark` 入口，更新所有用户文档中的启动命令。
- [x] 5.5 为运行中的评测实现 Escape 中断入口、控制器取消、部分报告落盘与回归测试。

## 6. 删除旧版与验证

- [x] 6.1 删除旧版服务端 HTML 页面、旧业务路由、`web-gui-v1.json`、复制展开逻辑及其失效测试，不保留兼容层。
- [x] 6.2 更新 README、Web GUI 评测文档和开发说明，写明 Node/Vite 构建要求、单进程运行方式、v2 任务分层与旧版移除。
- [x] 6.3 运行前端构建、`uv run ruff format src tests scripts`、`uv run ruff check src tests scripts`、相关 pytest 和 OpenSpec 严格校验。
- [ ] 6.4 在专用浏览器环境实际执行至少一次 10 条批次，保存报告并将真实浏览器/Agent 结果与离线合同测试分开记录。

## 7. 完整网站、真实导航与任务质量重构

- [x] 7.1 将 Vue 单表页面拆分为工作台、收件箱、项目、任务、日历、自动化、团队、报表和设置视图，并实现可点击的站内导航与选中状态。
- [x] 7.2 将所有任务统一从 `/arena/home` 启动，移除运行器按任务打开目标路由的行为，并记录仅由站内点击产生的导航检查点。
- [x] 7.3 扩展后端种子数据、业务实体和 API，使中高级任务能跨页面创建、检索、编辑、分配、移动、批量处理和确认。
- [x] 7.4 扩展任务 schema 与加载校验，强制 easy 1 至 3 步、medium 5 至 6 步、hard 7 步以上，并校验中高级任务的组合与跨页约束。
- [x] 7.5 重写 100 条自然、唯一且可直接发送给 Agent 的中文 instruction，删除机械占位文案，并以业务最终状态和必要检查点定义成功断言。
- [x] 7.6 补充统一入口、真实导航来源、各页面渲染、动作分层、自然语言唯一性和复杂状态评分的自动化测试。
- [x] 7.7 更新 README 与 Web GUI 评测文档，说明完整网站信息架构、导航约束、动作计数口径和任务指令规范。
- [x] 7.8 运行前端构建、Ruff、pytest 与 OpenSpec 严格校验，并在专用浏览器环境完成可见交互验证。
