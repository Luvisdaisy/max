## 1. 结构化会话协议与命令

- [x] 1.1 在 `console_core` 中定义带完善中文注释的封闭 `ConsoleEvent` 展示模型，将 `OperationResult` 和现有 dispatch 结果迁移为有序事件，并删除聊天输入在核心结果中的重复回显约定。
- [x] 1.2 集中定义受支持斜杠命令的名称、用法和说明，扩展 dispatch 以处理 `/help`、`/clear`、`/status` 及未知命令提示，同时保持 `/model`、`/doctor`、`/quit` 的既有语义。
- [x] 1.3 更新 `test_console_core.py`，验证事件类别与顺序、全部命令不触发普通聊天、`/clear` 和 `/status` 委托行为以及错误结果保持会话可用。

## 2. 本地运行时与净化进度

- [x] 2.1 为 `LocalChatRuntime` 增加只清除历史的公开操作和可选的加载、生成、就绪、失败状态通知，保持模型选择、懒加载和仅成功后提交历史的现有约束。
- [x] 2.2 扩展交互 dispatch，使 worker 可接收结构化进度回调，并为只读工具选择和执行生成仅含工具名、状态与耗时的白名单事件，不传递参数、截图、原始回执或模型选择提示。
- [x] 2.3 增加运行时和工具循环单元测试，验证清除历史后保留已加载模型、状态转换顺序、无工具时不产生工具事件，以及包含敏感假数据的回执不会进入任何展示事件。

## 3. Textual 会话工作台

- [x] 3.1 使用语义化消息 widget、受控 Markdown、紧凑状态栏和统一 TCSS 重构 Textual 布局，明确区分用户、MAX、命令、工具、提示与错误，并显示当前模型、运行状态和只读边界。
- [x] 3.2 将单行输入替换为多行编辑区，实现 `Enter` 发送、`Shift+Enter` 换行、空白忽略、`Ctrl+Up`/`Ctrl+Down` 进程内历史以及提交后的焦点恢复。
- [x] 3.3 基于集中命令清单实现 `/` 命令筛选菜单、键盘导航和补全，并让 `/clear` 清理 transcript 后恢复欢迎状态，让 `/help` 与 `/status` 使用独立命令结果样式。
- [x] 3.4 实现单活动请求门禁、阶段进度、递增耗时和 turn id 回调校验，移除通过 `exclusive=True` 隐式取消请求的行为，并在成功与失败路径统一恢复编辑区。
- [x] 3.5 实现位于底部时自动跟随、回看历史时保持视口及新内容提示，并在终端 resize 时应用紧凑布局，确保中文和长代码内容无需水平滚动即可阅读。

## 4. 前端验证与文档同步

- [x] 4.1 扩展 Textual `run_test()` 测试，覆盖初始焦点、多行键盘输入、命令菜单与补全、输入历史、忙碌期间单次 dispatch、完成/失败恢复和 Markdown 消息类型。
- [x] 4.2 增加滚动意图、紧凑布局、工具状态样式和敏感数据不进入 DOM 的回归测试，并确认斜杠命令测试不会加载本地模型。
- [x] 4.3 按当前实际行为更新中文 `README.md` 和 `tech-design.md`，清楚区分已实现终端体验与仍未实现的流式输出、取消、排队、会话持久化及完整权限系统。
- [x] 4.4 运行 `ruff format --check src tests`、`ruff check src tests`、`python -m unittest discover -s tests -v` 和 `python -m pip check`，并在交互式 Windows 终端人工核对启动、窄窗、多行输入、命令菜单和本地错误恢复。

## 5. 工具失败后的连续对话

- [x] 5.1 扩展只读工具解释结果，使无效选择、未授权工具、参数校验失败和工具执行失败都转换为字段受限的失败观察，并在最多一次工具尝试后继续最终回复生成。
- [x] 5.2 调整本地聊天衔接，使最终模型同时接收原始用户消息与净化失败观察；保留工具 `failed` 展示事件，并仅在最终回复生成失败时返回本轮错误。
- [x] 5.3 增加普通问候误选 `recognize_text`、未知工具、无效参数、工具不可用、无二次工具调用以及敏感参数和原始错误不进入最终提示、展示事件或 DOM 的回归测试。
- [x] 5.4 同步更新 `README.md` 与 `tech-design.md` 的已实现行为，并重新运行格式、静态检查、全量测试、依赖检查和 OpenSpec 验证。
